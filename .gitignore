node_modules/
dist/
dist-ssr/
*.local

# Env — never commit secrets
.env
.env.*
!.env.example

# Build artifacts
*.apk
*.aab
public/downloads/*.apk
android/app/build/
android/build/
android/.gradle/
android/local.properties
.gradle/

# Editor / OS
.vscode/*
.idea/
.DS_Store
Thumbs.db
*.sw?

# Logs
logs/
*.log
npm-debug.log*

# Test / coverage
coverage/
.vitest/

# Cloudflare
.wrangler/
.dev.vars

# Local scratch: the cloud bridge cannot delete files, so stale git lock
# files get moved here instead of removed.
_to_delete/

# The lockfile from the Kimi export is corrupt: `npm ci` rejects it and
# `npm install` crashes with "Exit handler never called". npm resolves fresh
# from package.json instead. Regenerate locally with `npm install` if wanted.
package-lock.json
