# r34: the dependency tree has a peer-dependency conflict that makes plain
# `npm install` crash with "Cannot read properties of null (reading edgesOut)".
# It broke the first Render build. Resolve peers the npm 6 way.
legacy-peer-deps=true
